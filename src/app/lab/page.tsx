import { LabClient } from './lab-client'

export const metadata = {
  title: 'Lab — TripGem Next-Gen Tools',
  description: 'Next-generation harm reduction tools for TripGem: advanced combination checker, dosage calculator, effect timeline, trip journal, and more.',
}

export default function LabPage() {
  return <LabClient />
}
